using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NumberStorage : MonoBehaviour
{
    // Start is called before the first frame update
    public int storage;
}
